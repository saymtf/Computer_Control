CREATE TABLE USERINFO.MyTabelle (ID INTEGER NOT NULL,
    MyKey INTEGER NOT NULL,
    MyValue VARCHAR(20) NOT NULL,
    MyTime INTEGER NOT NULL,
    PRIMARY KEY (ID)); 
    
insert into MYKILLERAPP.MyTabelle values (1,1,1, 'Hallo'); 